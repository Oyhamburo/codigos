class NPCs {
    constructor({
        name,
        description,
        code,
        faceset,
        chat
    }) {
        this.name = name
        this.description = description
        this.code = code
        this.faceset = faceset
        this.chat = chat
    }
}

export { NPCs }